---
workflow:
  mode: manual
  log_level: INFO

storage:
  backend: s3
  endpoint_url: REPLACE_WITH_EWC_S3_ENDPOINT
  buckets:
    raw_ifs: flexpart-raw-ifs
    preprocessed: flexpart-preprocessed
    model_output: flexpart-output
    plots: flexpart-plots

components:
  orchestrator_image: REPLACE_WITH_PINNED_IMAGE
  flexprep_image: REPLACE_WITH_PINNED_IMAGE
  flexpart_image: REPLACE_WITH_PINNED_IMAGE
  pyflexplot_image: REPLACE_WITH_PINNED_IMAGE
